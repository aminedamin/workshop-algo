ALGORITHM calcul
VAR
    tab : ARRAY_OF INTEGER[];
    tab1 : ARRAY_OF INTEGER[];
    i,j:INTEGER;
    s:INTEGER;
    s:=0;
    c :=0;

BEGIN
    FOR i FROM 1 TO tab.length STEP i++ DO
        FOR j FROM 1 TO tab1.length STEP j++  DO
            IF (T[i]=T[j]) THEN
                s := s+T[i]*2;
            END_IF
        END_FOR
    END_FOR


   FOR i FROM 1 TO tab.length STEP i++ DO
        FOR j FROM 1 TO tab1.length STEP j++  DO
            IF (T[i] = T[j]) THEN
              test = true
            END_IF
        END_FOR
        IF (test=false) THEN
        s := s+ tab [i];
        END_IF
    END_FOR
    Write(s);
    Write(c);
END